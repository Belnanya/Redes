package src.Redes;

import java.net.InetAddress;

class ClientInfo {
    private InetAddress address;
    private int port;

    public ClientInfo(InetAddress address, int port) {
        this.address = address;
        this.port = port;
    }

    public InetAddress




























































































































































































































































































































































































































































































    getAddress() {
        return address;
    }

    public int getPort() {
        return port;
    }

    @Override
    public boolean equals(Object obj) {
        if (obj instanceof src.Redes.ClientInfo) {
            src.Redes.ClientInfo other = (src.Redes.ClientInfo) obj;
            return this.address.equals(other.address) && this.port == other.port;
        }
        return false;
    }
}
